name: Run Journey Tests

on:  

  workflow_dispatch:
    inputs:
      test:
        description: "Choose test suite"
        type: choice
        required: false
        default: ''
        options:
          - ''
          - API
          - UI
      tag:
        description: "Choose test group"
        type: choice
        required: false
        default: ''
        options:
          - ''
          - Smoke
          - E2E
      branch-set:
        description: "Which branch set to test (develop or main)"
        type: choice
        required: false
        default: develop
        options:
          - develop
          - main

  workflow_call:
    inputs:
      test:
        required: false
        type: string
        default: ''
      tag:
        required: false
        type: string
        default: ''
      slack-webhook-url:
        required: false
        type: string

permissions:
  contents: read
  pull-requests: write

jobs:
  journey-tests:
    name: Run Journey Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:

      # Checkout all 4 required repositories
      - name: Checkout cads-tools
        uses: actions/checkout@v4.2.0
        with:
          repository: DEFRA/cads-tools
          ref: ${{ inputs.branch-set == 'main' && 'main' || 'main' }}
          path: cads-tools

      - name: Checkout cads-data-service
        uses: actions/checkout@v4.2.0
        with:
          repository: DEFRA/cads-data-service
          ref: ${{ inputs.branch-set == 'main' && 'main' || 'develop' }}
          path: cads-data-service

      - name: Checkout cads-mis
        uses: actions/checkout@v4.2.0
        with:
          repository: DEFRA/cads-mis
          # ref: ${{ inputs.branch-set == 'main' && 'main' || 'develop' }}
          ref: 'feature/ULITP-4787-aad-integration'
          path: cads-mis

      - name: Checkout cads-journey-tests
        uses: actions/checkout@v4.2.0
        with:
          repository: DEFRA/cads-journey-tests
          ref: ${{ inputs.branch-set == 'main' && 'main' || github.ref }}
          path: cads-journey-tests

      # Shell script permissions
      - name: Fix permissions for platform scripts
        run: |
          chmod -R +x cads-tools/**/*.sh || true
          chmod -R +x cads-data-service/**/*.sh || true
          chmod -R +x cads-mis/**/*.sh || true
          chmod -R +x cads-journey-tests/**/*.sh || true

      # Copy frontend.env into cads-mis
      - name: Copy frontend.env into cads-mis
        run: |
          mkdir -p cads-mis/compose
          cp cads-journey-tests/compose/frontend.env cads-mis/compose/frontend.env

      # Create cads-tools network early (other projects reference as external)
      - name: Ensure cads-tools network exists
        run: |
          if ! docker network inspect cads-tools >/dev/null 2>&1; then
            docker network create \
              --label com.docker.compose.network=cads-tools \
              cads-tools
          fi

      # FIX: Set ROOT_DIR so Localstack init scripts mount correctly
      - name: Set ROOT_DIR for compose mounts
        run: echo "ROOT_DIR=${{ github.workspace }}/cads-tools" >> $GITHUB_ENV

      # Debug networks
      - name: Debug networks
        run: docker network ls

      # Start platform (tools + backend + UI)
      - name: Start platform
        working-directory: cads-journey-tests/bin
        run: |
          chmod +x platform.sh
          ./platform.sh up

      # Allow for time for DB migrations etc
      - name: Wait for platform to stabilise
        run: sleep 15

      # Debug
      - name: Debug localstack
        if: always()
        run: docker logs cads-localstack || true
      - name: Debug LocalStack init (host)
        if: always()
        run: ls -R $ROOT_DIR/infra/localstack/init
      - name: Debug LocalStack init (container)
        if: always()
        run: docker exec cads-localstack ls -R /etc/localstack/init/ready.d || true

      # Backend logs
      - name: Backend logs
        run: docker logs cads_cds || true

      # Check Localstack init scripts
      - name: Check Localstack init scripts
        run: docker compose -p cads-tools exec -T cads-localstack ls -R /etc/localstack/init || true
      
      - name: Check Localstack init scripts 2
        run: docker inspect cads-localstack --format '{{ index .Config.Labels "com.docker.compose.project" }}'

      # List running containers
      - name: List running containers
        run: docker ps -a

      # Backend health logs
      - name: Backend health endpoint debug
        run: docker compose -p cads-tools exec -T cads_cds curl -v http://localhost:5555/health

      # Ensure report directories exist
      - name: Ensure report directories exist
        run: |
          mkdir -p cads-journey-tests/allure-report
          mkdir -p cads-journey-tests/playwright-report

      # Run journey-tests inside Docker
      - name: Run journey tests
        working-directory: cads-journey-tests
        env:
          TEST_INPUT: ${{ inputs.test }}
          TAG_INPUT: ${{ inputs.tag }}
        run: |
          FILTER=""

          if [[ -n "$TEST_INPUT" ]]; then
            FILTER="$TEST_INPUT"
          fi

          if [[ -n "$TAG_INPUT" ]]; then
            if [[ -n "$FILTER" ]]; then
              FILTER="$FILTER|$TAG_INPUT"
            else
              FILTER="$TAG_INPUT"
            fi
          fi

          echo "Running tests with filter: $FILTER"

          if [[ -z "$FILTER" ]]; then
            docker compose -p cads-tools -f docker-compose.yml up --build --abort-on-container-exit
          else
            docker compose -p cads-tools -f docker-compose.yml run --rm --build journey-tests \
              sh -c "npm ci && npx playwright test --grep='$FILTER'"
          fi
        
      # Debug networks (all)
      - name: Inspect cads-tools network
        if: always()
        run: docker network inspect cads-tools

      # Upload Allure report
      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: cads-journey-tests/allure-report

      # Upload Playwright report
      - name: Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: cads-journey-tests/playwright-report
          if-no-files-found: ignore

      # Collect Docker logs
      - name: Collect Docker logs
        if: always()
        run: |
          echo "Docker container logs" > logs.txt
          echo "=====================" >> logs.txt
          docker ps -a >> logs.txt

          for c in $(docker ps -a --format "{{.Names}}"); do
            echo "=====================" >> logs.txt
            echo "CONTAINER: $c" >> logs.txt
            echo "=====================" >> logs.txt
            docker logs $c >> logs.txt 2>&1 || true
          done

      # Upload docker logs
      - name: Upload docker logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docker-logs
          path: logs.txt

      # Stop platform (tools + backend + UI)
      - name: Stop platform
        if: always()
        working-directory: cads-journey-tests/bin
        run: ./platform.sh down

      # Slack notification
      - name: Post Slack Report
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ inputs.slack-webhook-url }}
          BUILD_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        working-directory: cads-journey-tests
        run: |
          if [[ -z "$SLACK_WEBHOOK_URL" ]]; then
            echo "Slack webhook not provided; skipping"
            exit 0
          fi
          node post-slack-report.mjs
